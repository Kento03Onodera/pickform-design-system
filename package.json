{
  "name": "pickform-design-system",
  "version": "0.0.0",
  "private": true,
  "description": "Pickform Design System — Monorepo",
  "scripts": {
    "build": "pnpm -r run build",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "style-dictionary": "^4.3.0"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  }
}
